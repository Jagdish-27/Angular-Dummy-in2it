import { AppCustomDirective } from './app-custom.directive';

describe('AppCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCustomDirective();
    expect(directive).toBeTruthy();
  });
});
