import { HighlighSearchPipe } from './highligh-search.pipe';

describe('HighlighSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlighSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
